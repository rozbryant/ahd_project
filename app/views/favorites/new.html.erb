<div class="page-header">
  <h1>Add Favorite</h1>
</div>

<% if @favorite.errors.any? %>
  <% @favorite.errors.full_messages.each do |message| %>
    <div class="alert alert-dismissable alert-danger">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      <%= message %>
    </div>
  <% end %>
<% end %>


<form action="/create_favorite" class="form-horizontal">
  <!-- Label and input for user_id -->
  <div class="form-group">
    <label for="user_id" class="col-sm-2 control-label">
      User
    </label>
    <div class="col-sm-10">
      <%= select_tag(:user_id, options_from_collection_for_select(User.all, :id, :name, @favorite.user_id), :class => "form-control") %>    </div>
    </div>

    <!-- Label and input for building_id -->
    <div class="form-group">
      <label for="building_id" class="col-sm-2 control-label">
        Building
      </label>
      <div class="col-sm-10">
        <%= select_tag(:building_id, options_from_collection_for_select(Building.all, :id, :name, @favorite.building_id), :class => "form-control") %>
      </div>
    </div>

  <!-- Label and input for created_at
  <div class="form-group">
    <label for="created_at" class="col-sm-2 control-label">
      Created at
    </label>
    <div class="col-sm-10">
      <input type="text" id="created_at" name="created_at" placeholder="Created at" class="form-control" value="<%= @favorite.created_at %>">
    </div>
  </div>

  <!Label and input for updated_at
  <div class="form-group">
    <label for="updated_at" class="col-sm-2 control-label">
      Updated at
    </label>
    <div class="col-sm-10">
      <input type="text" id="updated_at" name="updated_at" placeholder="Updated at" class="form-control" value="<%= @favorite.updated_at %>">
    </div>
  </div> -->

  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <input type="submit" value="Create Favorite" class="btn btn-primary">
    </div>
  </div>
</form>
